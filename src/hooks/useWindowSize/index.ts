export * from './useWindowSize';

// export const debounce = (fn, ms) => {
//   const t;
//   return (...args) => {
//     clearTimeout(t);
//     setTimeout(() => fn(...args), ms);
//   };
// };
//
// const handleClick = () => console.log(1);
//
// const debouncedHandleClick = debounce(handleClick, 300);
//
// window.addEventListener('resize', debouncedHandleClick);
